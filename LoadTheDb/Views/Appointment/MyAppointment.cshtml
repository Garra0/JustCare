@model IEnumerable<JustCare_MB.Dtos.AppointmentDtos.AppointmentDto>


<br />
<div class="container p-4 border">
    <div class="row pd-2">
        <div class="col">
            <h1 class="text-primary">my appointments list</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="Appointment" asp-action="index" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>Create new appointment</a>
        </div>
    </div>

    @* <form asp-action="Index" method="get">
        <div class="form-actions no-color">
            <p>
                Find by name, id or email: <input type="text" name="SearchString" value="@ViewData["CurrentFilter"]" />
                <input type="submit" value="Search" class="btn btn-default" /> |
                <a asp-action="Index">Back to Full List</a>
            </p>
        </div> 
    </form>*@

    <table class="table">

        <thead>
            <tr>
                <th>
                    DentistUserId
                </th>
                <th>
                    ArabicName
                </th>
                <th>
                    Date
                </th>
                <th>
                </th>
            </tr>
        </thead>

        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.DentistUserId</td>
                    <td>@item.CategoryArabicName</td>
                    <td>@item.Date</td>
                    
                    <td>
                        @* <a asp-controller="User" asp-action="Edit" asp-route-villaId="@item.Id" class="btn btn-success"> <i class="bi bi-pencil-square">Edit User</i> </a> *@
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-success"> <i class="bi bi-pencil-square">Edit User</i> </a>


                        <div class="col-5">
                            <form asp-action="Delete" asp-route-id="@item.Id">
                                <input type="submit" value="Delete" class="btn btn-danger form-control" />
                            </form>
                        </div>

                    </td>
                </tr>
            }
        </tbody>

    </table>

</div>





