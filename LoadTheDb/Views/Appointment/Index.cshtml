@model IEnumerable<JustCare_MB.Models.Category>

<h1>
    Dentist page
</h1>
<div class="col text-end pt-6">
    <a asp-controller="Appointment" asp-action="AcceptBooking" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>appointment booking</a>
</div>

<div class="col text-end pt-1">
    <a asp-controller="Appointment" asp-action="MyAppointment" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>My Appointments</a>
</div>

<br />
<div class="container p-4 border">
    <div class="row pd-2">
        <div class="col">
            <h1 class="text-primary">Category List</h1>
        </div>
        <div class="col text-end pt-1">
        </div>
    </div>


    @foreach (var item in Model)
    {
        <a asp-controller="Appointment" asp-action="Create" asp-route-id="@item.Id" class="btn btn-outline-primary">
            <i class="bi bi-plus-square-fill">
                <div class="card" style="width: 18rem;">
                    <img class="card-img-top" src=".../100px180/" alt="Card image cap">
                    <div class="card-body">
                        <h5 class="card-title">@item.ArabicName</h5>

                        @*                         <a href="#" class="btn btn-primary">Go somewhere</a>
                    *@
                    </div>
                </div>
            </i>
        </a>
        @*                         <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-success"> <i class="bi bi-pencil-square">Edit User</i> </a>
    *@

        @*  <div class="col-5">
    <form asp-action="Delete" asp-route-id="@item.Id">
    <input type="submit" value="Delete" class="btn btn-danger form-control" />
    </form>
    </div> *@

    }


</div>





